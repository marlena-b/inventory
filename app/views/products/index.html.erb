<% @products.each do |p| %>
  <%= p.name %>
  <%= link_to "Edit", edit_product_path(p), class: 'button' %>
  <%= link_to "Delete", product_path(p), class: 'button', data: { turbo_method: :delete, turbo_confirm: "Are you sure?" }  %>
<% end %>
