document.addEventListener("turbo:load", function () {
  // Reinitialize any bootstrap modals or other JavaScript dependent features.
  // For example:
  var myModal = new bootstrap.Modal(document.getElementById('add_products'), {})
});
